language: python
python:
  - "2.7"
  - "3.3"
# command to install dependencies
install:
  - pip install -r requirements.txt
  - pip install -r requirements_dev.txt
# command to run tests
script: trial cooperative.tests
